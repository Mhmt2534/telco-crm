spring:
  datasource:
    url: jdbc:postgresql://localhost:5438/billing_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    url: jdbc:postgresql://localhost:5438/billing_db
    user: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

app:
  kafka:
    topics:
      payment-events: "telcox.Payment.events"

