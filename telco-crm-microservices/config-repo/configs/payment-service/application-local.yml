spring:
  datasource:
    url: jdbc:postgresql://localhost:5442/payment_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    url: jdbc:postgresql://localhost:5442/payment_db
    user: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

payment:
  retry:
    delays-seconds: 10,20,30
