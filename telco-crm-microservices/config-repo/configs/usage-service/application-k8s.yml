server:
  port: 9006

spring:
  datasource:
    url: jdbc:postgresql://host.minikube.internal:5439/usage_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    url: jdbc:postgresql://host.minikube.internal:5439/usage_db
    user: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
