spring:
  datasource:
    url: jdbc:postgresql://product-db:5432/product_catalog_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    url: jdbc:postgresql://product-db:5432/product_catalog_db
    user: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  data:
    redis:
      host: redis
