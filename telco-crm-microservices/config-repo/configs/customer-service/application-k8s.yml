spring:
  datasource:
    url: jdbc:postgresql://host.minikube.internal:5434/customer_db
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
  flyway:
    url: jdbc:postgresql://host.minikube.internal:5434/customer_db
    user: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}

keycloak:
  server-url: http://host.minikube.internal:9011
  realm: telco-crm-realm
  client-id: telco-crm-client
  client-secret: B3Zlh6fORItPVGdybGkILIkH9S3hQgex

app:
  security:
    pii-encryption-key: ${AES_GCM_KEY}
