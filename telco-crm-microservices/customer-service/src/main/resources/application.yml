server:
  port: 9002

spring:
  application:
    name: customer-service
  config:
    import: "optional:configserver:http://localhost:8888"
  flyway:
    enabled: true
    baseline-on-migrate: true

app:
  security:
    pii-encryption-key: ${PII_ENCRYPTION_KEY:default-dev-key-32-chars-long!!!}

