apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: telco-crm
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 9009
      targetPort: 9009
