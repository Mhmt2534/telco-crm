apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: telco-crm
spec:
  type: ClusterIP
  selector:
    app: order-service
  ports:
    - protocol: TCP
      port: 9004
      targetPort: 9004
