apiVersion: v1
kind: Service
metadata:
  name: customer-service
  namespace: telco-crm
spec:
  type: ClusterIP
  selector:
    app: customer-service
  ports:
    - protocol: TCP
      port: 9002
      targetPort: 9002
