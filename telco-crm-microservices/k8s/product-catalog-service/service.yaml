apiVersion: v1
kind: Service
metadata:
  name: product-catalog-service
  namespace: telco-crm
spec:
  type: ClusterIP
  selector:
    app: product-catalog-service
  ports:
    - protocol: TCP
      port: 9003
      targetPort: 9003
