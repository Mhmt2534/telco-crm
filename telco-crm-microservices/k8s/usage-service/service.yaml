apiVersion: v1
kind: Service
metadata:
  name: usage-service
  namespace: telco-crm
spec:
  type: ClusterIP
  selector:
    app: usage-service
  ports:
    - protocol: TCP
      port: 9006
      targetPort: 9006
